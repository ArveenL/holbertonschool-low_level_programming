#include "main.h"

 /**
     * rev_string - reverses a string
     * @s: pointer to a character 
     *
     * Return: void
     */

void rev_string(char *s)
{
	int start = 0;
	int end = s[start]-1;
	int temp;
	

	temp = start;
	start = end;
	end = temp;

}
