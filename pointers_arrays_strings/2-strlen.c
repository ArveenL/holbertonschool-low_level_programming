#include "main.h"

/**
     * _strlen(char *s) - length of a string
     * @s: pointer to a character
     *
     * Return: integer
     */

int _strlen(char *s)
{
	int len = 0;

	while(char[len]!=\0)
	{
		len++;
	}

	return len;
}
