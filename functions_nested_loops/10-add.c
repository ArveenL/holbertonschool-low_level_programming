#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers
 * @a: first integer
 * @b: second integer
 *
 * Return: the sum of a and b
 */

int add(int a, int b)
{
	int result;

	result = (a + b);
	return (result);
}
